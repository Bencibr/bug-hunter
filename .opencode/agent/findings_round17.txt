[MEDIUM] CodegenToolCreator._validate_entrypoint 只查函数名不查签名 → 每次调用必崩的「已验证」工具被注册：LLM 生成 `def execute():`（无参）或 `def execute(**kwargs)`（仅 kwargs）时，名字校验 + AST 沙箱 + 安全扫描全部通过（test_cases 缺省为 None 时连沙箱执行都不跑）→ 工具注册并晋升 probation；运行时 execute(args) 抛 TypeError（0 positional arguments but 1 was given），LLM 拿到一个永远失败的「已验证」工具。src/neuroframework/capabilities/tools/creation.py:_validate_entrypoint。复现：_validate_entrypoint('def execute():\n return {"ok": True}') 返回 None（放行）。修复：校验入口函数必须能接收单位置 dict 参数（n_pos>=1 或 *args；**kwargs-only 同样拒绝）。TDD：test_entrypoint_requires_args_parameter 先红后绿。FIXED。
