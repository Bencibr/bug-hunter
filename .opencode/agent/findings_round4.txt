[HIGH] auto_memory L1 中句触发词剥离 bug（默认常开路径）：_extract_from_sentence 用 sentence.find(trig) 全句搜触发词，命中后 _strip_leading(sentence, sentence[:idx+len(trig)]) 把触发词之前的整段前缀剥掉 → 「以后记得写周报，下次周五交」被截成「周五交」（真实指令『写周报』永久丢失，且不被 L2 兜底，因为 L1 已产出一条）；「下次」「下次再说」裸触发被提取为持久指令入库并注入后续会话（噪声）。src/neuroframework/orchestration/tasks/auto_memory.py。复现：extract_durable_instructions([{role:user, content:"以后记得写周报，下次周五交"}]) 返回 ["周五交"]；ingest 后 build_memory_block 注入「下次」「周五交」。修复：中句触发词只剥触发词本身保留前置上下文 + 剥离后负向噪声检查（再说/只剩触发词）。TDD：test_mid_sentence_trigger_preserves_real_instruction / test_bare_or_hedged_trigger_is_noise_not_instruction 先红后绿。FIXED。
[HIGH] auto_memory L2 负缓存缺失 → 未入库候选每轮重判（v2.3.39 修复不完整）：round3 的 is_known 预筛只覆盖「已入库 confirmed/proposal」候选；被 LLM 判为非持久、未入库的候选句不在 _mem，每轮 _capture_auto_memory 重扫整段会话历史时被重复送 LLM judge（历史不变也重复同步调用内部 LLM，O(N²) 成本，阻塞轮末——真实会话「很多调用信息/上下文割裂/完成 27s」的机制之一）。src/neuroframework/orchestration/tasks/auto_memory.py。复现：3 轮同历史 ingest(llm_judge 计数) = 3 次 judge 调用（首轮后应 0 次）。修复：AutoMemoryManager._judged_not_durable 负缓存 + extract_durable_instructions on_judged 回调回传非持久候选。TDD：test_judge_not_called_again_for_judged_not_durable 先红后绿 + 性能 SLO test_ingest_repeat_history_negative_cache_p95_below_2ms（500 轮只 1 次 judge）。FIXED。
[MEDIUM] TaskScheduler._load 整批加载无逐条容错：持久化文件单条任务损坏（未知时区抛 ValueError / 非法 schedule 被加载为 parsed=None 永不运行）→ _load 整批 try/except 中断，坏条目之前任务幸存、之后任务静默丢失（定时任务重启后静默停摆）；非法 schedule 任务加载为 should_run 恒 False（静默永不运行）。src/neuroframework/orchestration/tasks/scheduler.py。复现：~/.neuro_scheduler.json 含 timezone="Mars/Olympus" 任务 → 该条目之后任务全部丢失（仅日志一行）。修复：逐条 try/except 容错 + parsed=None 跳过并告警。TDD：tests/modules/orchestration/tasks/test_scheduler_robustness.py 先红后绿。FIXED。
