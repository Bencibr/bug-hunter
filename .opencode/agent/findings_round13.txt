[MEDIUM] SqliteEventStore._rows_to_events 单条坏行让整个事件重放/投影崩溃：`float(row['timestamp'])` 等在 try 外（_fetch 只包查询不包转换）→ 事件库中单条脏数据（timestamp 非数值等）让 replay/replay_topic/claim_peek/SSE 续传/投影重建整个抛 ValueError——全部历史事件不可重放（一条脏行拖垮事件溯源）。src/neuroframework/orchestration/events/sourcing.py:_rows_to_events。复现：注入 timestamp='not-a-number' 的行后 bus.replay() → ValueError: could not convert string to float。修复：逐行容错——坏行跳过并告警，好行照常返回（与 scheduler/auto_memory 同类的部分失败恢复）。TDD：test_replay_skips_corrupt_row_keeps_good_events 先红后绿。FIXED。
