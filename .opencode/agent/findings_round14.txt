[MEDIUM] StreamableHttpTransport._parse_sse_body 多事件 SSE 响应丢最终结果：MCP streamable-http 一次请求可返回多个 SSE 事件（progress 通知 + 最终 result），旧实现解析到第一个 dict 立即 return → 调用方拿到 progress 通知（无 result 键）而非最终工具结果，工具调用结果静默丢失（返回空 result）。src/neuroframework/integrations/mcp/http_transport.py:_parse_sse_body。复现：SSE body 含 `data:{"jsonrpc":"2.0","method":"notifications/progress",...}` + `data:{"jsonrpc":"2.0","id":1,"result":{"content":"FINAL"}}` → 返回第一个（progress），FINAL-RESULT 丢失。修复：收集全部消息，返回最后一个带 id 的响应事件（method-only 通知跳过），无带 id 事件兜底取最后一条。TDD：test_streamable_http_sse_body_keeps_final_result_over_progress 先红后绿。FIXED。
