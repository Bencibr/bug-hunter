[MEDIUM] LLMGateway._parse_request 对 OpenAI 多模态消息崩溃：`"\n".join(m.get('content', ''))` 对 content 为 parts 列表（OpenAI 多模态标准格式，如 [{'type':'text','text':'分析图片'},{'type':'file','filename':'a.png'}]）抛 TypeError（sequence item 0: expected str instance, list found）→ 网关对多模态请求整个崩溃，无降级。interaction/channels/conversation_proxy.py:_parse_request。复现：LLMGateway(config).process_request({'messages':[{'role':'user','content':[{'type':'text','text':'分析这张图片'}]}]}) → TypeError。修复：列表 content 只提取 text part 拼入 prompt，附件/图片 part 跳过。TDD：test_conversation_proxy_parse_request_multimodal_parts 先红后绿。FIXED。
