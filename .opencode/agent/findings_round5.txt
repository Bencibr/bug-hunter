[MEDIUM] ConflictArbiter._resolve_by_priority 平局自胜自败：同角色（同优先级）动作并发写同一资源时，旧实现 `max`/`min` 用同一 key（-priority）都返回第一个元素 → Resolution.winner == loser（赢家自胜自败），且把 wait_and_retry 替代方案发给赢家自己（loser_action=winner_action）。src/neuroframework/governance/security/conflicts.py:159-173。复现：两个 role=executor 动作（timestamp 100/200）resolve priority → winner='agent-a' loser='agent-a'。修复：按 (priority desc, timestamp asc) 排序取首末，平局按先到先得破局。TDD：test_priority_tie_picks_distinct_winner_and_loser 先红后绿。FIXED。
[HIGH] experience_workshop.extract_experience 畸形 timestamp 崩溃：三处裸 `float(msg["timestamp"])`（:265/:282/:301），ISO 字符串/非数值 timestamp 抛 ValueError → run_workshop 整段崩溃 → _capture_experience_workshop 静默失效（坏消息每轮在历史里 → 经验沉淀永久关闭；且 curate_and_link/writeback/forked_review 整链丢失）。与 auto_memory v2.3.40 修复同类但未同步（同根因漏修的另一模块）。src/neuroframework/orchestration/tasks/experience_workshop.py。复现：extract_experience([{content:'部署报错了：ImportError', timestamp:'2026-08-13T10:00:00'}]) → ValueError: could not convert string to float。修复：_msg_ts() 容错降级缺省时间戳（三处替换）。TDD：test_extract_with_non_numeric_timestamp_does_not_crash 先红后绿。FIXED。
