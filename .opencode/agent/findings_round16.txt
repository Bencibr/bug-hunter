[HIGH] TrajectoryCompressor._sanitize_args 只脱敏顶层敏感键 → 嵌套密钥泄露进训练样本：旧实现仅检查顶层 key 是否命中 sensitive_keys，嵌套 dict/list（config.api_key、headers.Authorization、items[].token 等——工具调用参数最常见形态）原样保留 → 训练数据（JSONL 导出/微调）泄露 API key/token/凭据。src/neuroframework/intelligence/evolution/trajectory_compressor.py:_sanitize_args。复现：_sanitize_args({'config': {'api_key': 'sk-super-secret'}, 'headers': {'Authorization': 'Bearer tok-abc'}}) → 密钥原样保留。修复：_sanitize_value 递归脱敏（dict 逐键递归 + list 递归元素，敏感键名含 authorization/credential/access_token 等变体）。TDD：test_sanitize_recursively_redacts_nested_secrets 先红后绿（原钉住泄露行为的测试一并修正）。FIXED。
