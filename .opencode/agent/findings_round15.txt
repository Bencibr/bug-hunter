[MEDIUM] AsyncEventBus.drain_sync 在运行中的事件循环里对返回 awaitable 的普通 handler 调 asyncio.run() → RuntimeError 误标失败：drain_sync 的 async_deferred 守卫只检查 `inspect.iscoroutinefunction(h)`（handler 本身是协程函数），但「普通函数 handler 返回 awaitable」（适配器常见写法）会穿过守卫，进入 `asyncio.run(result)`——在已有事件循环内抛 RuntimeError → 事件被 dead-letter 误记为失败，handler 从未收到数据。src/neuroframework/platform/operations/events.py:drain_sync。复现：运行循环内 drain_sync，handler 为 def handler(data): return _inner()（返回 coroutine）→ errors=1, dead_lettered=1, received=[]。修复：in_running_loop 时对 awaitable 改用 asyncio.get_running_loop().create_task(result) 调度。TDD：test_sync_drain_in_running_loop_handles_awaitable_returning_sync_handler 先红后绿。FIXED。
