[HIGH] SecurityManager.decrypt_api_key 对 enc: 前缀在无 fernet 时把密文当 plaintext 返回：存的是 Fernet 密文（enc:...）但当前进程 cryptography 不可用（_fernet=None）时，`if encrypted.startswith("enc:") and self._fernet` 不成立 → 既不进 xor 分支也不进 plaintext 分支的判断被 `else` 兜住 → 返回密文本身 → 调用方把密文当真实 API key 使用（静默认证失败，且日志只留一条 debug「plaintext」误导排查）。src/neuroframework/governance/security/crypto.py:decrypt_api_key。复现：SecurityManager(secret_key='x') 且 _fernet=None → decrypt_api_key('enc:gAAAAA...') 返回该密文。修复：enc: 前缀在无 fernet 时 fail-closed 返回空 + 明确告警。TDD：test_enc_prefix_without_fernet_fails_closed_not_plaintext 先红后绿。FIXED。
