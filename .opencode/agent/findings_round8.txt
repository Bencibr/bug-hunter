[HIGH] restore_tree 超长会话恢复丢失最近消息：_restore_messages 用 list_records(limit=100)（timestamp ASC + LIMIT）取「最旧」100 条 → 会话 >100 条记录时，最近 N-100 条消息在 !resume/restore_tree 后从恢复上下文消失（续接的是对话开头而非结尾）。src/neuroframework/platform/persistence/session_store.py:372-393（_restore_messages）+ session_search.py:2990 list_records。复现：60 轮（120 条记录）后 restore_tree → ConversationManager 只含「用户问题 40~助手回答 49」，最近的「问题 50-59」丢失。修复：list_records 增 ascending 参数（False=取最近 N 条再反转为对话序），_restore_messages 改用 ascending=False。TDD：test_restore_tree_keeps_most_recent_messages_for_long_session 先红后绿。FIXED。
[HIGH] rebuild_messages_from_parts 并行 tool_call 覆盖：同轮并行发多个 tool_call（无中间 tool_result，现代 LLM 常见）时，`assistant['tool_calls'] = [...]` 把前一个覆盖成只剩最后一个 → 重建后前序 tool 消息引用不存在的 tool_call_id（OpenAI 协议违例，!resume 上下文损坏，下游 LLM/API 拒收或混淆）。src/neuroframework/platform/persistence/stream_parts.py:465-499。复现：delta + tool_call(A) + tool_call(B) + tool_result(A) + tool_result(B) → assistant tool_calls 只有 [B]，tool(A) 引用 call_A 无匹配。修复：tool_calls 追加而非覆盖。TDD：test_rebuild_parallel_tool_calls_keeps_all_tool_call_ids 先红后绿。FIXED。
