[HIGH] cli.py REPL 启动恢复路径 _restore_session_history 取最旧记录：continue_recent 恢复最近会话后，_restore_session_history 用 store.list_records(limit=50)（timestamp ASC+LIMIT）加载「最旧」50 条 → 会话 >50 条记录时，最近消息在重启续接后丢失（与 session_store._restore_messages 同类，v2.3.41 只修了 store 一处，cli 恢复路径漏修）。src/neuroframework/interfaces/user/cli.py:2582-2609。复现：40 轮（80 条）会话后 REPL 重启 → conversation_mgr 只含「问题 0~答案 24」附近，最近的「问题 39/答案 39」丢失。修复：list_records(ascending=False) 取最近 N 条。TDD：test_repl_resume_restores_most_recent_messages 先红后绿。FIXED。
[MEDIUM] _expand_file_refs 把邮箱域名/@提及当文件引用：正则 `@([^\s@]+)` 无词边界与路径形态过滤 → 用户输入含邮箱（me@example.com）或 @提及（@team）时，REPL 把它当文件路径调用 read_file_range（打印假「文件不存在」错误；若恰好存在同名文件还会把内容注入 prompt，非预期披露）。src/neuroframework/interfaces/user/cli.py:2496-2538。复现：_expand_file_refs('发邮件到 me@example.com') → read_file_range('example.com') 被调用。修复：@ 前加 (?<![\w]) 词边界断言 + 仅展开含 / 或 . 或 ~ 开头的路径形态引用。TDD：test_file_refs_do_not_swallow_email_addresses 先红后绿。FIXED。
