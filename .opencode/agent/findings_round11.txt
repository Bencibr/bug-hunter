[MEDIUM] _expand_file_refs 路径形态过滤误伤无扩展名真实文件（v2.3.41 修复引入的回归）：round 9 为排除邮箱域名/@提及给 @ 引用加了「含 / 或 . 或 ~ 开头」的路径形态过滤，但把无扩展名的真实文件引用（@Makefile / @Dockerfile / @LICENSE / @CHANGELOG）也一并拒绝 → 用户输入 `@Makefile` 不再展开文件内容（静默回归，README/构建文件等常见无扩展名文件无法用 @ 引用）。src/neuroframework/interfaces/user/cli.py:_expand_file_refs。复现：cwd 有 Makefile 时 _expand_file_refs('运行 @Makefile 目标') 原样返回，不展开。修复：非路径形态引用仅当对应文件真实存在（os.path.isfile）时才展开——邮箱域/@提及在文件系统上不存在 → 仍原样返回，两者行为并存。TDD：test_file_refs_expand_extensionless_existing_files 先红后绿。FIXED。
