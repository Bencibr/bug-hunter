[MEDIUM] TUI /memory 补全器功能漂移：MEMORY_SUBCOMMANDS 只含 search/list/forget，漏掉 v2.3.39 新增的 pending/confirm/reject 与既有 feedback/stats/show → `/memory pend<TAB>` `/memory conf<TAB>` 等完全无补全候选（CLI 实际支持这些子命令，Tab 补全却不提示，用户被迫手输）。src/neuroframework/interfaces/user/tui/completion.py。复现：completion_items('memory','pend') == []。修复：MEMORY_SUBCOMMANDS 对齐 cli.py memory 全部子命令（search/list/show/forget/feedback/stats/pending/confirm/reject）。TDD：test_memory_completer_covers_all_cli_subcommands 先红后绿。FIXED。
[MEDIUM] TUI KeymapFeed 和弦 leader 遮蔽单键绑定 + check_conflicts 漏报：默认 keys.show=ctrl+x（单键）存在时，用户配置和弦 ctrl+x ctrl+s → 首次按 ctrl+x 进入 partial 等待态（按键被吞进和弦等待，2s TTL 内不触发 keys.show）；check_conflicts 只检查多键序列的「末键」，leader 与单键绑定冲突完全漏报（检测器承诺「同一按键绑定到多个动作时报告」却静默）。src/neuroframework/interfaces/user/tui/shortcuts.py。复现：GlobalShortcutRegistry({'custom.save':['ctrl+x ctrl+s']}) → feed('ctrl+x')=='partial' 且 check_conflicts()==[]。修复：check_conflicts 增加和弦 leader 冲突检测（leader 与已注册按键冲突即报告）。TDD：test_conflict_detector_reports_leader_collision + test_chord_leader_shadows_single_key_binding 先红后绿。FIXED。
