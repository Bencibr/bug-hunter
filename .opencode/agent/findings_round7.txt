[MEDIUM] neuro cron run 不显示 shell 任务输出：cmd_cron 的 run 分支只读 result.get("result","")，而 scheduler._exec_shell 返回 {ok, exit_code, stdout, stderr, duration_ms}（无 result 键）→ shell_exec 任务（最常见 action）执行后只打印「✓ 任务执行完成」，stdout 永远不显示。src/neuroframework/interfaces/user/cli_commands.py。复现：cron add t 'every 1min' --args '{"command":"echo HELLO"}' + cron run <tid> → 仅显示「✓ 任务执行完成」，无 HELLO。修复：按 stdout/output/result 逐键取输出（stderr 兜底）。TDD：test_cron_cli_run_displays_shell_stdout 先红后绿。FIXED。
[HIGH] LongTaskManager.create_task 字符串 task_type 崩溃：日志行 `task_type.value`（src/neuroframework/orchestration/tasks/long_tasks.py:501）在调用方传字符串 task_type（如 task_type="shell"——LongTask.__post_init__ 显式支持字符串转换，属合法 API）时抛 AttributeError：任务已落库注册并触发状态回调，调用方却收到异常（状态不一致：任务存在但创建"失败"）。复现：create_task('x', task_type='shell') → AttributeError，且 list_tasks() 含该任务。修复：日志改用已转换的 task.task_type.value。TDD：test_create_task_accepts_string_task_type 先红后绿。FIXED。
[MEDIUM] neuro task stats 永远显示全 0：cmd_task 的 stats 分支读 total_tasks/running/completed/failed，而 LongTaskManager.get_stats() 返回 total_in_registry/active/total_completed/total_failed → 无论有多少任务，「总任务/运行中/已完成/失败」全部显示 0（用户无法从 CLI 看到真实任务统计）。src/neuroframework/interfaces/user/cli_commands.py。复现：创建+完成 1 个任务后 neuro task stats → 全部 0。修复：读取实际键名。TDD：test_task_cli_stats_reads_real_stats_keys 先红后绿。FIXED。
