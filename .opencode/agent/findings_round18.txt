[MEDIUM] ExperienceAdvisor「同命令此前失败」的 caution 在 before_execution 被静默丢弃：should_proceed 对 exact_failures（相似度>0.8 的同命令此前失败）返回 should_proceed=True + confidence=0.4 + reason「Same command failed N time(s) before. Proceed with caution.」——但 ExperienceFeedbackLoop.before_execution 只在 should_proceed=False 时把 advisor reason 加入 warnings → 用户重跑已知失败命令时零警告（caution 设计在唯一消费者里不可达，命令静默再跑）。src/neuroframework/application/experience/feedback.py:before_execution + advisor.py:should_proceed。复现：9 相似成功 + 1 同命令失败 → should_proceed=True, reason 含 "failed ... before" → before_execution 的 warnings 为空。修复：confidence<0.5 的 caution 也作为 warning 展示（仍放行，不阻断）。TDD：test_before_execution_surfaces_exact_failure_caution 先红后绿。FIXED。
