[MEDIUM] StrategyExtractor._extract_common_steps 用步骤总数误判相位覆盖：旧实现 `if len(steps) >= len(experiences)*0.7` 把「相位步骤总数」对比「经验数×0.7」——单条经验贡献多个同相位步骤（如 1 条经验 4 个 execute 步骤、其余 4 条无 execute）即可让该相位被误判为「≥70% 经验共有」，提炼出代表「1/5 经验」的 common 策略步骤（策略沉淀被单条长经验污染）。src/neuroframework/application/experience/core.py:_extract_common_steps。复现：5 条经验（1 条含 4 个 execute 步骤）→ execute 相位被提取为 common 步骤。修复：按「含该相位的去重经验数」判定（phase_experiences 集合），≥70% 经验有该相位才算 common。TDD：test_extract_common_steps_counts_experiences_not_step_totals 先红后绿（既有 70% 语义测试保持绿）。FIXED。
